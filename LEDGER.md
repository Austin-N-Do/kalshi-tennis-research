# Research Ledger

Append-only index of every experiment. **Never edit a completed result in
place** — supersede it with a new entry and mark the old one SUPERSEDED.
Governed by [RESEARCH-CONSTITUTION.md](../RESEARCH-CONSTITUTION.md); procedure
in the `research-ledger` skill.

Statuses: PROPOSED · PREREGISTERED · RUNNING · PASSED · FAILED · INCONCLUSIVE · SUPERSEDED

## Index

| ID | Title | Status | Date | Result (one line) |
|---|---|---|---|---|
| G-1 | Leakage/parity guardrail tests | PASSED | 2026-07-07 | 13 tests; leak canary mutation-verified |
| G-2 | Rolling-origin + paired bootstrap in evaluator | PASSED | 2026-07-07 | paired CI resolves ~1e-4; lefty ablation clean null 3/3 |
| G-3 | Score parser + golden table | PASSED | 2026-07-07 | 99.988% parse on 281k rows; 543 unmarked RETs found |
| G-4 | Outcome backfill + CLV scoreboard | PASSED | 2026-07-07 | 14,176 graded; ITF closing proxy flagged unreliable |
| EXP-1 | Margin-aware Elo × smooth-K × inactivity | PASSED | 2026-07-07 | games-margin + smooth-K shipped; 0.604→0.594 LL |
| EXP-2 | Qualifier block (5 features) | FAILED | 2026-07-07 | ITF signal but tour CI regressed fold 1 → no-regression rule |
| EXP-3 | Availability trio | INCONCLUSIVE | 2026-07-08 | passes only as full trio, gain ~−.0015; ship held |
| EXP-8 | Market edge — model vs Kalshi close | **FAILED** | 2026-07-09 pre-reg · read 2026-08-07 | **Red light triggered.** Tour clean n=1,348: model LL 0.6303 vs market 0.5904, Δ +0.0398 CI [+0.0207,+0.0604]. Pre-registered rule: no real money on this signal |
| EXP-9 | Exit plan (stops / model-shift / TP) | INCONCLUSIVE | 2026-07 | shipped to paper; realized stops −$1,569 over 192 fires |
| EXP-10 | Exit-policy replay harness | PASSED (validation) · gate RE-CLEARED 100% on W32+ · **arms RUN 2026-08-28 → INCONCLUSIVE** | 2026-07 · re-run 2026-08-14 | replay reproduced 85/86 realized stop exits. **Re-validation on the full 402-trade population FAILS the gate at 79.9% (<90%), so the four-arm comparison did not run and no arm results exist.** Not a regression: `ground_truth` provenance reproduces 158/158 (100%), price-only stop path 121/124 (97.6%); the failure is confined to the `reconstructed` orientation-guess path (42/101, 41.6%). Cause is the documented `live_score_snapshots` defect — `p_a_live` is stored without recording which ticker `player_a` was — plus checkpoint fields being unpopulated on early trades (W27: 7/66 vs W28+: ~100%). Weekly reproduction climbs 60.6% → 100% as instrumentation lands. Restricting to passing provenance (279/282 = 98.9%) is post-hoc and not outcome-neutral; requires a declared amendment. **Root cause already fixed in production:** `live_score_snapshots.ticker_a` went 0% (W28–W30) → 36% (W31) → **100% (W32, W33)**, which is exactly why weekly reproduction reaches 100% from W31. W27–W30 trades are permanently unreplayable (orientation never recorded, unrecoverable); W32+ trades replay at ~100%. **The blocker is sample accrual, not code** — W31+W32 gives only 64 replayed trades vs a design needing 39 favorite-band with a low-teens discriminating subset. Disposition: hold the comparison until the W32+ population is powered, then run with an outcome-neutral `entered_at >= W32` date restriction declared in advanc **DISPOSITION 2026-08-28 — DORMANT, NOT DEAD. Deliberately not resumed; nothing changed, no setting touched.** Recorded because the capability is worth keeping in view: **replay is the only mechanism here that can evaluate exit policies THAT WERE NEVER DEPLOYED.** Shadow fires (EXP-17's method) require the rule to be wired up and left running; a live A/B (EXP-18's method) costs a cohort slot and months of accrual. Replay can rank several untried policies over the same historical trades at once — which is exactly what `compare_arms()` was built for and why it should not be deleted. **Its blocker has resolved itself:** the `live_score_snapshots` orientation defect that reduced the reconstructed path to 42/101 (41.6%) was fixed in production, and orientation is now recorded on **295/295 exit trades across W32-W35 (100%, four consecutive weeks)** — so a re-validation restricted to W32+ would very likely clear the >=90% gate that blocked the four-arm comparison. W27-W30 trades remain permanently unreplayable (orientation was never written down; unrecoverable). **Corpus preservation verified, not assumed:** `scripts/prune_price_history.py` never prunes ticks for any ticker appearing in `trades` at any age — the code comments name this explicitly as 'the exit-replay corpus' — so the deferred storage/partitioning plan cannot destroy replayable history for traded markets, and the two-tier retention (tour 120d / ITF 45d) only ever touches never-traded markets. **Why it is not being resumed now:** the cohort ladder answers the questions currently on the table with real fills rather than reconstruction — lab2 vs lab3 isolates stop-loss + model-shift, lab3 vs lab5 isolates take-profit (EXP-18). A live A/B beats a simulation when both are available. **What would justify resuming it:** a policy question no live cohort can answer — ranking multiple untried exit rules simultaneously, or testing a rule without spending a cohort slot on it. Re-validation would need an outcome-neutral `entered_at >= W32` restriction declared in advance, per the 2026-08-14 disposition **GATE RE-RUN 2026-08-28 on the pre-declared W32+ restriction: PASS at 100.0% (158/158), vs 79.9% on the full population.** The restriction was declared in this ledger BEFORE the run (see the disposition above) and verified after: 158 trades, earliest `entered_at` 2026-08-04 (ISO W32), latest 2026-08-28 -- outcome-neutral, a deployment date, discarding every arm equally. Per rule: `exp9_model_shift` 36/36, `exp9_take_profit` 26/26, `exp9b_take_profit` 53/53. Per side: NO 69/69, YES 89/89. Timing error median 0.0s (mean 3.2s, p90 3.6s); price error median 0.00c (mean 0.06c, p90 0.00c). Weekly 100% across all four weeks in range. **The failure mode is not worked around -- it is gone.** The reconstructed orientation-guess path that scored 42/101 (41.6%) now drives **0/158 decisions (0.0%)**; decisions come from ground truth 115/158 (72.8%) and the price-only path 43/158 (27.2%), with `mc_ambiguous=0` and real `ticker_a` on 8,752/8,752 MC ticks. Ground-truth checkpoints on 158/158 trades (5,571 total, avg 35.3/trade). **Code touched: `validate()` gained a `--since` argument (read-only research script; no trading code, no settings, no cohort config).** Cosmetic: the script's `[6d]` by-week header labels the first in-range week W31 where ISO numbering says W32 -- a labelling convention in the report only, the underlying population is W32+ as verified above. **STILL NOT UNBLOCKED for results.** Two things stand between this and a four-arm comparison, and the gate was only one: (1) `compare_arms()` is a scaffold that raises `NotImplementedError` -- it has never been written; (2) its open semantic question is **unresolved and is explicitly the user's to settle**: for TP-on arms (A, C), should `exp9b_take_profit` be FORCED executable on `bot_hold` (testing 'what if this cohort had exp9b'), or stay gated by real `TP_EXECUTABLE_SOURCES` membership (bot_hold is not a member, so exp9b would never fire and arms A/C would collapse into their TP-off twins)? The pre-registration's 'TP ON = both rules enabled' reads as the former, but the docstring is explicit this must be confirmed, not assumed. **No arm results exist and none may be presented until both are settled.** What this re-run buys is a proven instrument: replay is now trustworthy on W32+ data, so the capability is ready when a question needs it -- ranking multiple untried exit policies at once, or testing one without spending a cohort slot **ARMS RUN 2026-08-28 — INCONCLUSIVE, which the pre-registration names as 'a valid and expected outcome' at this N. No config change; no trading setting touched.** The two remaining blockers were cleared: (1) the open semantic decision is **SETTLED as FORCED** — for TP-on arms `exp9b_take_profit` is forced executable rather than gated by real `TP_EXECUTABLE_SOURCES` membership. **The pre-registration resolves this itself:** its arm table gives arm B's mechanism as `tp_executable=False`, which is only a distinguishing lever if arm A has it True; `bot_hold` is not a member, so under the gated reading arms A/C would collapse into B/D and the four-arm comparison would be a two-arm one. (2) `compare_arms()` is now implemented, hard-gated in `main()` so arms cannot run or print unless `validate()` clears — enforced in code, not left to the caller (a first cut had `validate()` not propagating its result and the arms correctly suppressed themselves; fixed). Population: **103 replayable `bot_hold` trades** (65 favorite band, 38 other; 3 skipped ungraded), fixed $25/trade, settlement-ordered equity clock. **PRIMARY (B vs A, favorite band, paired): n=65 of which only 30 actually differ; paired mean +$0.80, median +$0.00, 90% CI [-0.48, +2.02], MPE $1.50. CI includes 0 and the median does not favour the challenger → INCONCLUSIVE.** Split-half signs were consistent (+0.50 / +1.09) but that alone does not license adoption. Descriptive, not a hypothesis test: **REF Hold is the best arm overall (+$118.74 vs A -$91.62, B -$63.14, C +$17.44, D +$6.64)**, consistent with the exit-stack findings elsewhere in this ledger; in the favorite band every arm is slightly negative and REF Hold is least bad (-$8.67). **FIDELITY CAVEAT, material for any future run: threshold provenance is `settings_store_fallback` on 103/103 trades.** `bot_hold` never exits by rule, so it has no `exit_context` to read thresholds from, and the arms are therefore evaluated at TODAY's dial values rather than the dials in force when each trade was live — exactly the silent post-hoc substitution `_thresholds_from_context` warns about. Re-runs after any dial change are not comparable to this one. Per the decision rule, INCONCLUSIVE means run both coherent configs forward and accumulate real evidence — which lab2/lab3/lab5 are already doing — and explicitly does NOT license a judgement call from the point estimate. **The instrument is now complete and reusable:** `python -m scripts.replay_exit_policies --since YYYY-MM-DD --arms`. |
| EXP-11 | Trade-conditional calibration / market blend | **SUPERSEDED** | 2026-07-31 | claimed blend beats model 3/3 folds → superseded by EXP-13 |
| EXP-12 | Live-entry veto counterfactual | PASSED | 2026-07-31 | vetoed subset worse in all 5 cohorts; veto now enforced |
| EXP-13 | Blend vs **market** control arm | **FAILED** | 2026-08-07 | Supersedes EXP-11. At spread ≤10¢ the blend is indistinguishable from market alone (CIs straddle 0, 3/3 folds); model coef decays 0.22→0.06. Model adds no measurable info at executable quotes |
| EXP-14 | Live-state information beyond prior + price | **FAILED** | 2026-08-07 pre-reg · amended 2026-08-16 · **read 2026-08-28** | Harness `scripts/exp14_live_state.py` validated against known-signal and known-null synthetic data. Dry-run on live data: **n=47 vs 300 required — NOT A RESULT.** Two amendments, both declared before any result: (1) M-recal control replaces the raw ask; (2) **counted sample restricted to `>= 2026-08-15`** — see below. Spec: [EXP-14](../experiments/EXP-14-live-state-information.md **READ 2026-08-28 at n=304/300 — FAIL.** Population 304 matches (one row each), 2026-08-15 -> 2026-08-28, walk-forward 3 folds of eval n=60. **The live coefficient is NEGATIVE in all three folds (-0.048, -0.055, -0.026)**; the criterion requires positive in every fold, so the failure is decisive on the condition that could actually be measured. Market coefficient +1.017/+1.097/+1.083 — the market does all the work and slightly more. Live-only arm is not close: logloss 0.5948/0.4547/0.7893 vs market 0.4013/0.3906/0.4515. The blend (B) does edge M-recal in 2/3 folds but by 0.002-0.005 **with a negative live coefficient**, so those margins come from refitting the market term, not from live information — the EXP-11 trap in miniature, caught by the pre-registered coefficient-sign condition. **Fifth consecutive edge null (EXP-8/13/15/16/14):** the earlier four showed the model loses to the market pre-match; this one shows in-play state does not rescue it. **DESIGN FLAW, recorded for the next spec: the CI condition was UNEVALUABLE at the committed sample.** The harness needs n>=200 per eval fold to bootstrap; every fold printed `n<200, no CI`. The 300 target was sized on the TOTAL pool, but the walk-forward consumes it in eval folds of 60 — satisfying condition 1 would have needed ~1,800+ matches, 6x the target. The lesson is to size by the unit the test consumes, not the pool (EXP-19 dodges this by sizing clusters with sequential looks). Strength caveat: at eval n=60 those small negative coefficients are imprecise, so the honest statement is **'no positive contribution in any fold'**, not 'live state is harmful'. **The instrument freeze declared 2026-08-16 is now LIFTED** — feed, MC, serve stats and re-sim behaviour were held still pending this read. |
| EXP-17 | ITF exit rules (stop-loss + model-shift) | **PREREGISTERED · COLLECTING** · unit amended 2026-08-22 | 2026-08-21 | Cohort `bot_lab4` (ITF-only, stop OFF, model-shift OFF, TP ON), registered and **PAUSED**; zero trades at preregistration. Tests whether removing the two rules beats running them, using the cohort's OWN `shadow_fires` as the counterfactual — no second arm, no added multiplicity. Primary: paired per-trade P&L (actual − shadow), clustered by match, 95% bootstrap CI excluding 0, minimum practical effect **$1.50/trade**. **Sample committed at 400 matches BEFORE any data; no interim peeking** (the 100-match figure assumes lab2's winner's-curse-inflated +0.140 and is not the planning number). Bar is abs(z)>1.96 — one pre-committed hypothesis on data that does not yet exist, so no multiplicity penalty. Motivated by: exit stack costing **+$757.71** across 376 closed trades (4/5 cohorts), and the mechanism that `adverse_shift`=0.15 sits below tennis's 0.368 median intra-match swing while +0.093 entry overconfidence eats 62% of the trigger. **NOT an edge test** — EXP-16 showed the model loses to ITF pricing by more than to tour. **Amendment 2026-08-22, declared before any outcome was examined:** unit of analysis moves from `match_id` to `event_ticker` — `kalshi_markets.match_id` is populated on 2,262/3,314 tour markets but **0/10,732 ITF**, so '400 matches' and 'clustered by match' were literally uncomputable for this cohort. `event_ticker` identifies one h2h match uniquely (EXP-16 hit the same wall). Target 400, clustering, and pass/fail all unchanged. Caught at 19 trades. Spec: [EXP-17](experiments/EXP-17-itf-exit-rules.md) |
| EXP-16 | ITF edge at executable prices | **FAILED** (RUN 2, 2026-08-18) | 2026-08-14 pre-reg · RUN 1 invalid · RUN 2 read 2026-08-18 | **RUN 1 produced no result — the control arm broke.** M-recal was built on `fit_temperature`, the family EXP-15 had just shown cannot correct a shift; T railed to the bound (5.000) in all 4 cells and recalibration made the market strictly worse (ITF 0.5404 → 0.6338; TOUR 0.6188 → 0.6658), so the model was compared against a crippled baseline. Fix for RUN 2: Platt (slope + intercept), plus a preregistered sanity gate — if `logloss(M-recal) > logloss(MARKET raw)` the run is INVALID. Criteria unchanged. **Descriptive only, not results:** model loses to the raw ask in both segments (ITF 0.6147 vs 0.5404; TOUR 0.6802 vs 0.6188); on the ITF edge-gated cell (n=1,065/1,062 ev) the model's favoured side wins **0.3559** vs market-implied 0.3777 and model-claimed 0.4706 — i.e. picks underperform the market's own implied rate, the opposite of the less-efficient-ITF hypothesis. Also recorded: `live_score_snapshots` holds **445 tour events and ZERO ITF**, so any live ITF work needs archiving widened first. **RUN 2 (Platt control, sanity gate passed in the primary cell): FAIL — ITF primary logloss(M-Platt) − logloss(MODEL) = −0.0871, 95% CI [−0.1076, −0.0669], entirely market-side; gap larger than EXP-8's tour gap (0.040). Thin-ITF-inefficiency hypothesis unsupported. Three hairline (≤0.17%) gate breaches in non-primary cells disclosed in the spec; verdict unchanged under any reading. Fourth consecutive null: EXP-8/13/15/16. `bot_tour_only` stays; ITF expansion justified for sample accrual only** |
| EXP-16 | (preregistration detail) | — | 2026-08-14 | Design: the tour-only nulls (EXP-8/13/15) have never been tested on ITF. Tests whether thin ITF books are less efficient. ITF n=6,937 / 3,482 events vs TOUR n=1,768 / 884 (TOUR runs as a method control that must reproduce the null). Control arm is the **walk-forward recalibrated market (M-recal)**, not the raw ask, per EXP-14's amendment. Dedup-then-filter order pinned in advance after EXP-15 deviated on it. A pass licenses a forward test only — **not** a `bot_tour_only` change. Spec: [EXP-16](experiments/EXP-16-itf-edge.md) |
| EXP-15 | Trade-conditional calibration (temperature) | **FAILED** | 2026-08-13 | Temperature scaling cannot fix the defect. On the bot's own gates (pre-start, spread ≤10¢, edge 5–15¢): n=4,233 over 3,263 events, chronological 2/3–1/3 split. TEST realised YES rate **0.3883**; model said 0.4782 (**+0.090 overconfident**), market ask said 0.3889 (**bias +0.0006 — the market is calibrated where the model is not**). Fitted T railed to the optimiser bound (5.0) and made it worse: logloss RAW 0.5890 → RECAL 0.6553, Δ −0.0665 CI [−0.0849, −0.0475]. **Diagnosis: the miscalibration is a shift, not a sharpness problem; temperature has no intercept so it pushes toward 0.5, away from truth.** Replicates the +0.088 overconfidence measured on 419 outcome-linked trades (127 matches) on an independent ~10× sample. Deviation recorded: edge filter applied inside the per-ticker dedup, so rows are the *first crossing* of the edge threshold (optional-stopping bias) — n=4,233 rather than the 1,361 from filtering after dedup. Spec: [EXP-15](../experiments/EXP-15-trade-conditional-calibration.md), harness `scripts/exp15_trade_conditional_calibration.py` |
| EXP-18 | Hold-only arm for the lab2 entry family | **PREREGISTERED · COLLECTING** | 2026-08-27 pre-reg · live 2026-08-28 | New cohort `bot_lab5` = `bot_lab2`'s entries mirrored **in code**, `exits_enabled=False`, absent from `TP_EXECUTABLE_SOURCES`. Completes the ladder lab2 (full stack) -> lab3 (TP only) -> lab5 (no exits), so **lab3 vs lab5 differ in exactly one rule** and take-profit gets a live control instead of a counterfactual. Primary: paired per-trade P&L (lab5 - lab3) on `event_ticker`s both entered, clustered by `event_ticker`, 95% bootstrap CI excluding 0; **MPE $2.00/trade** (measured exit fees alone are ~$1.20/trade, so anything below that is paying a fee to do nothing). **Sample committed at 200 matched pairs BEFORE the cohort existed** - sd of the paired delta on lab3's 61 graded trades is $9.77, so n=(2.8*9.77/2.00)^2=187; 200 carries margin. At lab3's 3.43 trades/day that is ~58 days, read late Oct 2026. No interim peeking; bar abs(z)>1.96 (one hypothesis, data did not exist). **The observed +$2.44/trade is NOT the planning number** - it is the sample that motivated the test, and the 2026-08-22 entry already records lab2's edge failing multiplicity. **A FAIL is the likely outcome and is stated in advance:** splitting all 170 graded exp9b exits by date gives -$7.55/trade (t=-4.30) in Jul 18-Aug 4 but -$2.42 (t=-1.00) and -$2.19 (t=-0.84) after; ex-fees the two recent windows are -$1.23 and -$0.97, i.e. zero. Fold 1 sold at 77.4c and settled 91.2% - a 13.8c gift that has not recurred; recently 74-77c sold vs 77-81% settled, a ~3c gap that is the fee. Weakest joint declared up front: both cohorts cap at 10 open / 25% exposure and holding extends position life 1.12x, so lab5 will sometimes be full when lab3 is not - **unmatched pair count and direction will be reported whatever it says**. Tests `exp9b` AS CONFIGURED, not take-profit in general (2026-08-21 blind spot: `p_live_pos>=0.5` guard, collapses to a ~69.4c price rule in degenerate deciding sets at aggressiveness 40). The sub-60c/above-80c band structure is **post-hoc, excluded from pass/fail**, recorded so it cannot later be passed off as preregistered. Does not disturb EXP-17 (independent books; no lab4 setting touched). A PASS licenses disabling exp9b **for the lab2 entry family only** - not a global exit change, and no claim about entry edge (lab2 t=1.47, lab3 t=1.41, top-2 of 7 cohorts, consistent with selection noise). **Auto-trade switched ON 2026-08-28 with zero trades on the books** — the spec was registered the day before the cohort existed, so every observation is post-preregistration. Spec: [EXP-18](experiments/EXP-18-hold-only-lab-arm.md) |
| EXP-19 | lab2-family entry edge at executable prices | **PREREGISTERED · COLLECTING** | 2026-08-28 | **Changes nothing** — analysis preregistration over cohorts already running (no trading code, no gates, no cohort config). Tests whether bot_lab2's entry filter (edge>=0.11, underdogs, 25-50c, tour) beats the price it pays. Motivated by a retrospective read of **+16.0 pts** (implied 36.6c vs 52.6% actual, n=133, t=3.72) that **contradicts EXP-15**, which found the model +0.090 OVERconfident against a calibrated market on n=4,233 — 30x the sample, opposite sign. Retrospective data cannot settle it: it generated the hypothesis, and **the gate-search history is unrecorded** (the 2026-08-22 entry already logs lab2's edge failing multiplicity once), so all 135 retrospective clusters stay descriptive. **Counted sample is forward-only from 2026-08-29.** Unit = one row per `event_ticker` — lab2/lab3/lab5 share entries to the decimal, so a match traded by several cohorts is ONE observation, not three; ~3.4/day, no pooling speed-up. Exits are irrelevant to the metric (entry price + side + settlement only), so EXP-18's arms all contribute without interfering. **PRIMARY (gating):** pooled group-sequential, O'Brien-Fleming at n=100/200/300, z>=3.471/2.454/2.004, overall two-sided a=0.05; PASS at first crossing, FAIL at n=300 uncrossed, <100 clusters is NOT A RESULT. **MPE 8 pts** (adverse selection here is 4-7 pts, so below ~7 nothing survives execution). **SECONDARY (reported, NOT gating):** three DISJOINT 100-cluster blocks, independent so no correction, >=2/3 same-direction significant = persistent — the decay check. Both readings declared in advance precisely so neither can be cherry-picked. **Simulated operating characteristics (sd=49.4, 2000 runs): PASS rate 0.027 at true edge 0, 0.802 at 8 pts, 1.000 at 16 pts** — calibrated as designed. Realistic earliest crossing is look 2 (~59 days): replaying the retrospective sample, +15.64 at n=100 gives z=3.16, short of the stringent 3.471 first boundary. Executable-price discipline enforced in code — `entry_price` must BE the ask lifted (verified 100% of 135 retrospective, median spread **1.0c**, midpoint match 0/206), harness prints a failure banner below 90%. Decay reported descriptively at every look, never gating: retrospective slope **-0.016 pts/day, CI [-0.902, +0.870], P(slope<0)=0.514** — the apparent +19.6/+14.5/+12.4 fold decline is NOT a detectable trend, though the CI still admits decay fast enough to erase the edge in <3 weeks, which is why the design is sequential rather than fixed. A PASS licenses belief in the entry filter on forward data and **nothing more — explicitly not a capital-deployment decision**. Harness `scripts/exp19_entry_edge.py` (rule in `passes_criterion()` so it cannot drift), validated against the retrospective window: reproduces +15.87 pts CI [+7.65,+24.36] exactly. Spec: [EXP-19](experiments/EXP-19-lab2-entry-edge.md) |
| EXP-20 | Stop-loss + model-shift on the lab2 entry family | **PREREGISTERED · COLLECTING** | 2026-09-03 | **Changes nothing** — governance over two arms already running (`bot_lab2` vs `bot_lab3`), no code, no cohort config, no dials. Closes the gap where **the LARGER leak had no preregistration while the smaller one got a dedicated cohort**: retrospectively on lab2's 162 closed trades, holding instead of exiting gains **$422.62 from model-shift (n=58) + $160.85 from stop-loss (n=49) = $583**, against take-profit's **$112.40** — 5.2x more. lab2 and lab3 resolve to byte-identical entry gates and differ only in `stop_loss_enabled` / `model_shift_enabled`, so this is a live A/B, not a counterfactual, and cap displacement sits inside the measurement. **Bundled treatment: a PASS indicts the PAIR, not either rule** — attribution needs a fourth arm and is out of scope. Primary: paired per-trade P&L (lab3 - lab2) on `event_ticker`s both entered, clustered, 95% bootstrap CI excluding 0; **MPE $2.50/trade** (above EXP-18's $2.00 because two rules fire far more often — 68 of 98 retrospective pairs differ — bundling more fee-bearing round trips per observation). **Sample committed at 200 matched pairs BEFORE any counted data**; sd of the paired delta on 98 retrospective pairs is $11.79, so n=(2.8*11.79/2.50)^2=174, and 200 carries margin. Counted sample is **forward-only from 2026-09-04**. At ~3.4 pairs/day that is ~59 days, read early Nov 2026. No interim peeking; bar abs(z)>1.96. **The retrospective +$2.39/trade (t=2.01) is NOT the planning number** — it is the motivating sample, it sits barely above threshold, and the 2026-08-22 entry already records lab2's edge failing multiplicity. **Prior differs from EXP-18's:** here the effect is larger and the mechanism clearer — stop-loss sells at an average **-24c against entry** and its gap widened monotonically from -13.7c to -29.7c (firing later and later), while model-shift keys on a model that EXP-8/13/15/16/14 all show loses to the market at executable prices, so it should be expected to sell into correct prices. **But the exits are not uniformly wrong** — on `KXWTAMATCH-26AUG30KEYKOR` all three cohorts entered YES at 29c; model-shift sold lab2's at 28c for -$1.77 while lab3/lab5 rode it to settlement at 0 for -$14.00 each, and the same rule threw away $22 and $30 on two other matches that week. **NOT independent of EXP-18** — `bot_lab3` is an arm in both, so the two results share a book and neither may be described as replicating the other. Unmatched pair count and direction reported with the result (cap occupancy: lab2 10.7% of hours, lab3 19.4%). A PASS licenses disabling both rules **for the lab2 entry family only** — the config lab3 already runs — not a global exit change and no claim about the entry edge (EXP-19). Spec: [EXP-20](experiments/EXP-20-stop-and-shift-lab-arm.md) |
| EXP-21 | Position-cap capacity arm on the lab2 entry family | **PREREGISTERED · COLLECTING** | 2026-09-03 | New cohort `bot_lab6` ("Lab 6 (Hold+)") = `bot_lab5` EXACTLY with `max_open_positions` 20 (from 10) and `max_exposure_pct` 35% (from 25%), so **lab5 vs lab6 differ in CAPACITY and nothing else** — entries and sizing still mirror bot_lab2 in code, both hold to settlement. Motivated by a real constraint: labs are cap-bound, NOT signal-bound (lab3 turns away ~14.4 qualified candidates/tick against 10 slots, ~40% capture; lab5 ~46.8/tick and sits at cap 42.7% of hours; **lab4 has ZERO cap blocks** — signal-limited, the opposite regime). **Caps sized from a hold-only replay of lab2's entry stream: the position cap stops binding at 14 (peak reached 13), and peak exposure was only $179.88 = 18% of bankroll** — so the cap was binding on COUNT, never on money, and raising exposure alone would have achieved nothing. Also settles a standing question: **the account is never close to fully deployed — even uncapped, peak deployment is 18%.** 20/35% is deliberately above the 14 implied, because the replay can only see trades lab2 ACTUALLY took and the cap-blocked pool is invisible to it. Primary: paired per-trade P&L (lab6 - lab5) on `event_ticker`s both entered, clustered, 95% bootstrap CI excluding 0, **MPE $2.00**; secondary (reported, NOT gating) the trades lab6 took that lab5 could not, with their realized edge. **Sample committed at 200 matched pairs before any data**, ~59 days, read early Nov 2026, no interim peeking. **A FAIL is the likely outcome and is stated in advance:** candidates that qualified while lab2 sat at its cap realize **+6.2 pts (n=72, SE~5.8, CI through zero)** against **+13.3 pts** for those taken anyway — roughly half quality. Estimated value if all 72 had been taken at lab2's $13.95 average stake: **+$115.80 (+11.5% ROI)**, real if the edge is real and indeterminate if not. Limitations recorded up front: capacity **amplifies whatever sign the entry edge has**, so a PASS here alongside an EXP-19 FAIL is contradictory and licenses nothing; `bot_lab5` is the control in both EXP-18 and this, so the results share a book and neither replicates the other; and a 2x position cap scales drawdown exposure roughly proportionally against realized maxima already at bot_lab -$389 on a $1,000 basis (39%). A PASS licenses raising the cap **for the lab2 entry family only, and only if EXP-19 has not failed**. Spec: [EXP-21](experiments/EXP-21-capacity-arm.md) |
| EXP-22 | Entry selection vs the model as the source of edge | **PREREGISTERED · COLLECTING** | 2026-09-03 | **Changes nothing** — analysis governance over cohorts already running. Asks the question the five nulls leave open: the model does not beat the market ANYWHERE, including on the lab2 filter's own population (logloss **0.7920 vs 0.6628**, n=1,685), yet the sides the bot BUYS win **52.5%** while sides it declines from the same gate cell win **37.2%**. Specification oriented to the side backed: `won ~ logit(p_model,side) + logit(p_market,side) + ENTERED`. **Already ruled out, so not under test:** entry TIMING (taken trades enter at **+0.36c** vs the first qualifying price, sd 2.87, identical on 60/144, 45 cheaper / 39 dearer, avg 2.7h later — the bot buys different candidates, not better moments) and the MODEL (coefficient negative and insignificant in every fit). PASS = ENTERED coefficient positive with abs(z)>1.96; below the committed sample it is NOT A RESULT. **Sample committed at 225 entered candidates before any counted data**: se on the coefficient scales ~2.14/sqrt(n_entered) (calibrated from se 0.1819 at n=139), so 80% power at the **0.40 log-odds MPE** needs 224. Forward-only from 2026-09-04, ~3.3 entered/day, read mid-Nov 2026, no interim peeking. **Simulated operating characteristics (2,000 runs each): PASS rate 0.022 at true beta 0, 0.411 at 0.25, 0.805 at 0.40, 0.991 at 0.62.** **DESIGN DEFECT CAUGHT BEFORE REGISTRATION, recorded because it is EXP-14's failure mode repeating:** the first draft required the point estimate to ALSO exceed the MPE, which caps power at 50% by construction — at a true beta of exactly the MPE half of all estimates land below it. Simulation gave 0.488 power where the z-only rule gives 0.805. The MPE now sizes the sample and is NOT a second hurdle. **The retrospective +0.6191 (z=+3.40, n=1,948) is NOT the planning number** — it is the generating sample after many analyses on the same data. Independence verified: both sides of a match can never qualify at once (needs `yes_bid - yes_ask >= 0.22`, impossible), so no anti-correlated pair enters. Conservative bias declared: the not-entered pool includes cap-blocked candidates, which are quality-neutral and dilute the contrast. **Limitations:** ENTERED is a BUNDLE (qual, liquidity, EXP-12 veto, history gate, cooldown, cap availability) — a PASS says selection works but **cannot say which gate earns it**, which needs the skip instrumentation; and this is not a randomised control (the decisive design is a cohort entering a RANDOM qualifying candidate, which needs trading-path code and is out of scope mid-collection). **Relationship to EXP-19:** EXP-19 would PASS if selection works but cannot separate selection from model skill; they run on overlapping streams so neither replicates the other. Both pass -> edge is in the gates; EXP-19 passes and this fails -> edge real but unexplained; EXP-19 fails -> this is moot. Harness `scripts/exp22_entry_selection.py`. Spec: [EXP-22](experiments/EXP-22-entry-selection.md) |

## Superseded chain

- **EXP-11 → EXP-13.** EXP-11 omitted the market-only control (Constitution
  Rule D) and did not filter on spread (Rule E). Its population had a median
  spread of ~80¢, so "the market" it beat was the midpoint of an untradeable
  book. EXP-13 adds both controls and reverses the conclusion. EXP-11's
  numbers remain on record and are correct *as computed*; the inference drawn
  from them was wrong.
- **All pre-2026-07-05 model metrics → void.** Tournament-date ordering leak;
  see [model-analysis-2026-07.md](../model-analysis-2026-07.md) §1. The
  "fatigue > Elo" importance finding belongs to this class.

- **G-4 divergence-CLV readings and the 13 snapshots through 2026-08-02 →
  SUPERSEDED.** They were calculated mid-to-mid across every earlier scan of
  a leg, with no spread filter or CI. The preserved values are tagged
  `mid_pseudoreplicated_v1` → `exec_ask_v2` → `exec_ask_v3`.
  The five v2 rows remain unchanged; v3 uses better-edge selection, the first
  tradable quote, and one event per observation. `exec_ask_v3` is the current
  historical method, though its reads remain descriptive and cannot ship the
  filter. This supersedes the method, not G-4's closing-line score.

## Infrastructure events (not experiments)

| Date | Event |
|---|---|
| 2026-09-03 | **Randomised-entry arm PROPOSED and deliberately NOT built** (`docs/proposals/randomised-entry-arm.md`). A `bot_rand` cohort applying the HARD gates (tour, h2h, 25-50c, edge>=0.11) then picking **uniformly at random** among survivors, bypassing the discretionary stack (qual grade, liquidity, EXP-12 live veto, history gate, cooldown, arrival priority), holding to settlement. Against `bot_lab5` that is the **causal** effect of the discretionary gates, where EXP-22 can only measure an observational association — `ENTERED` is six bundled gates and conditioning on it is post-selection. **Its standing value exceeds any single experiment:** every future entry-selection change is currently argued against a retrospective baseline the change may itself have been tuned on; a live random arm is a fixed uncontaminated reference that accrues whether or not an experiment is running. Primary: realised edge in points, UNPAIRED two-sample (lab5 - rand) clustered by `event_ticker`, 95% bootstrap CI excluding 0; unpaired is unavoidable since the arms take different candidates by construction, which is what makes it a control and also what makes it slow. **Sizing at sd~49 pts: 1,046/arm at a 6pt MPE (~300 days), 376 at 10pts (~110 days), 167 at 15pts (~50 days) — proposed at 375/arm and a 10pt MPE**, since the ~15pt retrospective gap is winner's-curse inflated. **NOT BUILT because unlike lab5 and lab6 it cannot be expressed through existing parameters** — it needs a new branch in the auto-entry loop in `scanner/scan_service.py`, the loop whose documented hazard at line 875 stopped auto-trade for ALL cohorts for ~50 minutes on 2026-08-21. Five experiments are mid-collection and an outage is non-neutral for EXP-18 (lab5 at cap 42.7% of hours vs lab3's 19.4% would unmatch pairs asymmetrically). Build requirements recorded: per-cohort flag defaulting off with a captured/reproduced baseline across every cohort (the 2026-08-21 Step 3 method), **seeded RNG persisted on the trade** (an unreproducible control is not a control), draw AFTER the hard gates so both arms face the same universe, bounded to one draw per tick per slot, shipped alongside the skip instrumentation since the secondary metric needs it. **Expected outcome stated in advance: the random arm SHOULD lose money and by more than lab5** — that is the point of a control, not a reason to stop early; if it MATCHES lab5 the discretionary gates are decoration and the +16pt anomaly is almost certainly a tuned filter yet to revert. Budgeted cost: ~375 trades at roughly the base rate, an expected drawdown in the low hundreds. Sequenced after EXP-18 and EXP-19 read, same gate as the skip instrumentation. |
| 2026-09-03 | **CORRECTION + new descriptive finding: the five edge nulls were NEVER measured on the lab2/lab3 population, and a claim of mine that they contradicted it was OVERSTATED.** Checked against each spec: EXP-8 (tour closing lines), EXP-13 (tour, spread <=10c), EXP-14 (tour live snapshots), EXP-16 (ITF vs tour) are all whole-population. **EXP-15 is the closest and still differs on three material axes**: its edge gate is `0.05 <= (p_model - yes_ask) <= 0.15`, i.e. the **YES side only** — lab2 bets NO on ~58% of trades, so most of lab2's population was never in EXP-15's sample; it is **pre-start only** while lab2 enters in-play on ~1/3 of trades; and it has **no price band** while lab2 is underdogs at 25-50c. The repeated phrasing 'lab2's +16pts contradicts EXP-15 on 30x the data' is therefore wrong and is retracted — they measure different populations and are not in direct contradiction. **The null DOES extend to lab2's population, now measured for the first time** (descriptive, retrospective, NOT preregistered): on lab2's exact gates fired on EITHER side, tour, spread <=10c, one row per ticker, n=1,685 — **logloss model 0.7920 vs market 0.6628, a 0.129 gap, ~3x EXP-8's tour-wide 0.040**. Model mean bias is fine (+0.0023 vs market +0.0115); it is DISCRIMINATION that is worse. **The result that matters, split by whether the bot actually entered:** ENTERED (n=146) logloss model **0.6891** vs market **0.7269** — **the model BEATS the market by 0.0378**; NOT ENTERED (n=1,539) model **0.8018** vs market **0.6567** — loses by 0.1450. A **0.18 logloss swing between the two subsets.** Note the market is also unusually BAD on the entered subset (0.7269 vs 0.6567), so the bot appears to enter where the market is uncertain AND the model is right. **All figures are settlement-based; exits never enter them, so this is already the hold-only result.** Consistent with the earlier measurement that candidates in lab2's gate cell realize 0.0pts at first qualifying observation while trades actually taken realize +15.0pts. **HEAVY CAVEATS:** conditioning on entry is post-selection; n=146; this is the same sample that generated the hypothesis; not preregistered; and the entry decision bundles gates (qual, liquidity, EXP-12 live veto, timing) that cannot be separated without the per-candidate skip logging that is specced but not built. **Implication for EXP-19:** it measures realized edge vs executable price and remains valid as written — it is agnostic to mechanism — but the mechanism it would be confirming is probably ENTRY SELECTION, not model quality. That distinction should be stated when it reads. Nothing changed: no code, no config, no dials. |
| 2026-08-22 | **api-tennis key cleared; Business trial allowed to lapse.** `API_TENNIS_KEY` emptied in `.env` (kept as an empty assignment, NOT deleted — see below). Rationale: EXP-16 RUN 2 killed the edge case, and on the segment that matters SofaScore carried **15 live ITF matches to api-tennis's 1**, so it was never the better ITF source. Its one genuine strength — bulk serve-stats enrichment without SofaScore's 8s throttle — was already spent clearing the backlog (253 matches, now 1 pending). **Nothing breaks:** `live_engine._enrich` calls `_enrich_api_tennis` first and it returns False on empty/failure, falling back to the SofaScore round-robin automatically. **What degrades:** in-game points and the serving indicator go from one bulk call covering every live match to 2 per-match SofaScore calls/tick under an 8s global limit — with ~40 live ITF that is a ~140s full cycle instead of near-immediate. Sets and games are unaffected (SofaScore bulk feed). Second-order: the live MC re-sims on state change, so coarser points mean some triggers fire late — a quality degradation for EXP-14/EXP-17 live state, not a stoppage, and neither experiment's criteria depend on point-level granularity. **Latent trap closed:** `get_score_provider()` checks only that the key is a non-empty string, not that it works, so a STALE key would have selected the api-tennis provider and returned `[]` — reporting no live matches — instead of falling back. The key must therefore stay EMPTY rather than stale. (`/live/scores` is orphaned — no frontend or API caller — so the trap was latent, not active.) Verified after restart: provider resolves to `SofaScoreScoreProvider`, `/live/scores` returns `source: sofascore`, cards 200, poller fresh, zero api-tennis references in the log. |
| 2026-08-22 | **`exp9_shadow_edge_gone` measured retrospectively — executing it would LOSE money. Do not re-propose without new evidence.** The rule fires when the sellable bid exceeds the model's fair value by >=5c and is shadow-only (`# logged, never executed (Phase 1)`). It looked like the mirror of the entry rule (buy underpriced / sell overpriced) and was proposed as the most promising exit change on the table. **Measured on all 232 closed trades carrying a fire (166 matches): selling at the first fire turns +$808.89 into +$574.30, delta -$234.59, mean -$1.02/trade, 95% CI [-2.05, -0.02] clustered by match — CI excludes zero on the HOLDING side.** Better on only 40/232 (17%); median delta 0.00. **Why the framing was wrong:** it compares the bid to the MODEL's fair value, not to entry, so it fires on losers too (observed: entry 30c -> fire 26c; entry 35c -> fire 12c). It is an 'the model has given up' signal, not a profit-taker. Deeper reason it cannot work here: a sell rule keyed on model fair value inherits the model's errors, and EXP-8/13/15/16 all show the model loses to the market at executable prices — so it systematically sells into correct prices. **Caveats:** retrospective and NOT preregistered (same evidential class as the lab2 edge that failed multiplicity); CI upper bound is -0.02, i.e. fragile; heterogeneous by cohort (bot_lab +$23 vs bot_lab3 -$157, the latter confounded because lab3 already has TP executable and no stop/shift, so an early sell preempts working take-profits); only the FIRST fire is recorded, so this tests 'sell at first signal', not the best available exit. **Nothing was changed** — no config, no rule flipped, lab3/lab4 undisturbed. Triggered by a user-reported trade (`KXWTAMATCH-26AUG20ANIPEG-PEG`) where 80c was available and the position settled at 0, $36.23 left on the table — real, but the tail rather than the pattern. |
| 2026-08-21 | **exp9b take-profit blind spot found; two History-tab display bugs fixed.** User flagged a lab3 trade where the take-profit never fired. Investigation: `KXWTAMATCH-26AUG20ANIPEG-PEG` (NO, entry 26c) held a sellable bid at/above the exp9b trigger for **176 consecutive ticks, peaking at 80c**, then settled at 0 for **-$12.03** against ~**+$24** available — a ~$36 swing. **Cause is not a bug:** exp9b requires `p_live_pos >= 0.5` and the MC held the position at **0.42-0.46** the whole time (market 80c vs model 43%), so the rule refuses to bank exactly when the market disagrees with the model. In a deciding set the MC conditionals go degenerate (1.0/0.0) and exp9b collapses to a price rule firing at `100/(1+tp_ratio)` = 69.4c at aggressiveness 40. **Material to EXP-17**, whose cohort has exp9b as its ONLY executable exit — recorded in the spec; 'take-profit ON' does not mean profits are reliably banked. **Deliberately NOT fixed:** retuning `tp_aggressiveness` would not clear the `p_live_pos` guard anyway, and lab3/lab4 are mid-experiment. **Retracted en route:** an intermediate diagnosis claimed `p_a_if_lose_next` was NULL — that was a `(x or -1)` falsy-zero bug in the diagnostic script; the value is 0.0 and the column is populated on all 62,093 rows. **Display fixes (frontend only):** Kalshi returns two title shapes ('Will X win the A vs B: R16 match?' and 'X wins'); only the long form was parsed, so short-form markets fell back to the 3-letter ticker suffix and a NO position rendered as `NO · PEG` — reading as if Pegula was backed when the position backed her opponent. `botYesPlayer` now parses both shapes, `botOppCodeFromTicker` recovers the opponent from the event ticker, and the badge leads with the player actually backed (`NO · ANI`), with the contract mechanics kept in the tooltip. lab4 also added to the six hardcoded frontend cohort lists that predated it (it was absent from the History source selector entirely). |
| 2026-08-21 | **`market_price_history` storage optimisation NOTED and DEFERRED** (no code changed). Measured with ITF archiving live: 8,575,200 rows / 1,854 MB, of which **indexes are 896 MB (48%)** — `market_price_history_pkey` alone is 346 MB for a UUID `id` with **no foreign keys pointing at it** (verified against `pg_constraint`) and no lookup use; it and the `TimestampMixin` columns duplicating `recorded_at` are inherited boilerplate worth **~600 MB (32%)**. Growth ~600k–900k rows/day (72k/h peak, 1–5k/h overnight) → steady state **~12–18 GB**, which **supersedes two earlier under-estimates this session (3.5–4 GB, then 5–5.5 GB)**. Against 80.8 GB free this is not a constraint. **Deferred by user decision:** the table IS the exit-replay corpus that EXP-10's replay and EXP-17's shadow counterfactual reconstruct from, EXP-17 is mid-collection, and a mis-migration would destroy evidence for matches that already happened. Plan (column drop → monthly partitioning → Parquet cold archive) and the rejected options (USB / cloud for the hot path) are in [DEFERRED-price-history-storage.md](../proposals/DEFERRED-price-history-storage.md), with revisit triggers: <30 GB free, >40M rows, EXP-17 complete, or a prune proving slow. Note the prune has **never run against real volume** — it reports 0 prunable because nothing has aged past either cutoff yet. |
| 2026-08-21 | **Step 3: per-cohort series admission + `bot_lab4` (ITF).** `bot_tour_only` was enforced once globally in `_automatic_filters`, discarding ITF from the shared `auto_bets` pool before any cohort was consulted — so no cohort could opt in. Replaced with two per-cohort gates on `EntryGates` (`tour_enabled` / `itf_enabled`) resolved by the same `_effective()` chain as the region toggles; `itf_enabled` defaults from `bot_tour_only`, so **every pre-existing cohort is unchanged with no config edit**. `bot_lab4` = lab3's config differing in exactly three keys (paused, tour off, ITF on) — **ITF-ONLY**, chosen over a single `allow_itf` flag (user) because 'may also trade ITF' would have mixed populations inside one cohort and destroyed the tour-vs-ITF contrast. **Equivalence verified**: baseline captured on pre-refactor code, re-applied, all six existing cohorts IDENTICAL; lab4 takes 2 ITF / 0 tour. Suite 379 green. Two defects caught by the process: (1) the first gate tested 'not ITF' and so admitted 71 outrights as tour — correct only by filter ordering; both sides are now positive checks; (2) `resolve_quality` initially short-circuited on the persisted alias cache, reporting 100/100 unique on a slate that independently measured 94/6 — `_record_alias` fires on ambiguous guesses too, so an alias laundered an old guess into a confident answer and silently disabled the ITF identity gate. Both have regression tests. **ITF identity gate added** (`bot_itf_require_unique_name`, default True): ITF entries require both players to resolve uniquely, measured cost 6% of ITF sides; tour unaffected. Corrected premise: the proposal's 'require pinned `sofa_id`' gate would have rejected **nothing** — 100/100 live ITF sides are already pinned; the 47.9% figure covered all 14,177 DB players, mostly historical records absent from live markets. lab4 remains PAUSED pending EXP-17. |
| 2026-08-21 | **Research archiving widened to ITF (Step 2).** `_RESEARCH_SERIES` gained `KXITFMATCH`/`KXWITFMATCH`, so price + score paths are now archived for every scanned ITF h2h market, not just tour. Motivation: `live_score_snapshots` held **445 tour events and ZERO ITF** purely because of a two-entry whitelist — ITF live-state cannot be collected retroactively. **Archiving only: `bot_tour_only` unchanged, ITF remains untradeable, no new upstream calls** (ITF cards were already fetched and enriched each tick). Paired with a **two-tier retention** — new `itf_price_history_retention_days = 45` vs tour's 120 — because ITF is 3.3x tour market count (10,544 vs 3,214 active) and would have taken `market_price_history` from 1,815 MB toward 6-8 GB on an 82.6%-full disk; the 45d tier caps it near 3.5-4 GB. The never-prune-if-traded rule is unchanged. **Verified live:** first 10 minutes archived 2,040 ITF rows across 102 ITF tickers (vs 800 tour rows / 8 tickers), and `live_score_snapshots` went 0 -> 4 ITF events. **Two unit tests in `test_research_archiving.py` were updated, not deleted** — they asserted the old tour-only scope; they now assert ITF is in scope and outrights remain out. Suite green at 362. **EXP-14 is unaffected** (its population filters to tour by ticker prefix). Caveat: retention has still never run against real volume — the prune reports 0 prunable because nothing has aged past either cutoff yet. |
| **2026-08-16** | **EXP-14 counted sample restricted to `>= 2026-08-15`; live path frozen until it reads.** Declared before any result (Rule I). Three instrument changes landed inside the collection window that began 2026-08-08: the api-tennis feed switch (08-09), the serve-stats backfill (08-10→08-14, 1,988 matches re-stated via api-tennis plus ~13k via SofaScore), and `live_resim_budget_s` (08-12). All 161 qualifying matches accrued on a `p_a_live` that moved under them. Every change made the instrument *better*, so a pooled read would mix a staler early signal with a fresher later one and a null could not be separated from dilution — forfeiting the decisiveness the experiment exists for. The cut is outcome-neutral (a date fixed by deployment history, chosen before any arm was scored, discarding both arms equally). Clean sample at declaration: **37 of 300**, ~10 further days at ~26/day. Earlier rows stay on record as descriptive. **Freeze:** no discretionary change to feed, MC, serve stats, re-sim behaviour or scoring until the read; breakage may be repaired, and any such change restarts the counted sample from its date. Prompted by evaluating an ITF archiving widen — rejected on evidence: the re-sim budget has never bound, peak load is ~14 live events/hour, so ITF crowds out nothing, and EXP-16's descriptive read already points against ITF edge |
| 2026-08-09 | **Score archiving PRODUCTION-EXERCISED — supersedes the 16:14 UTC "UNKNOWN" row below.** First tour matches went live after the widen: 1,217 score rows across **8 tour events**, of which **6 had zero bot trades** (invisible to research before the widen — the selection bias is broken in practice, not just in principle). **EXP-14 accrual is now MEASURED, no longer unknown:** ~8 tour events archived per day, ~87% clearing the EXP-14 filters → **~7 qualifying matches/day, ≈6 weeks to n=300**. Consistent with the preregistration's 4–6 weeks. Qualifying funnel: 85 tour tickers with orientation → 54 qualifying all-time → **7 post-widen** (the leakage checklist admits post-widen rows only). Caveat: one day of data, and the tour calendar varies — re-measure in a week before planning around it. |
| 2026-08-09 | **Serve-stats backfill — justification corrected before it entered the record.** A proposal argued the backfill was foundational because "the live MC engine has no real serve information at all — it's Elo re-expressed as a serve rate." **That premise is false.** `prediction_service.py:394` computes `snap.serve_pts_won + adj/2` — the real rolling serve rate *plus* an Elo adjustment; 0.62 is a fallback only for an empty window. Measured on the exact EXP-14 population: of **388 active tour players, ZERO have an empty serve window**; mean 11.5 real serve matches in the rolling 20, 60.9% window coverage. (All players incl. ITF: 9.1% empty.) The backfill is still worth doing — 60.9% coverage has room, it is regression insurance (EXP-5), and the dataset goal stands alone — but **not** because the live MC lacks serve inputs. Recorded so the false rationale is not inherited. |
| 2026-08-08 | **Post-widening archive verification (observed through 16:14 UTC).** `market_price_history` added 118,788 snapshots across 26 tour contracts; `live_score_snapshots` added zero rows. `match_start_events` recorded 22 first-seen live events, all ITF, and none had a bot position. This is expected: unheld ITF remains outside the widened ATP/WTA scope. The score archive is unit-tested but has not yet been production-exercised for an unheld tour match. EXP-14's post-widening qualifying rate and completion timeline remain **UNKNOWN**. |
| 2026-08-08 | Divergence-CLV measurement corrected: one first qualifying pre-start quote per leg, executable favored-side entry, ≤10¢ spread, and one-sample bootstrap CI. Prior midpoint pseudo-replicated readings superseded; no trading decision or gate change made. |
| 2026-07-05 | Ordering leak fixed; all prior metrics voided |
| 2026-08-07 | Research constitution, 5 skills, ledger, and test fence established |
| 2026-08-07 | Test fence `PROTECTED_FROM = 2026-07-01` created; `evaluate_features` fences by default. **Fenced baselines (gate against these): XGB 0.59330 LL / 0.7489 AUC; logistic 0.59645 LL / 0.7495 AUC** — all 21 features, 20% single split, ~237.5k samples. The pre-fence 0.594 XGB figure is close but was measured on a different dataset |
| 2026-08-07 | `backtesting/engine.py` quarantined (fabricated ROI; see audit §4 F5) |
| 2026-08-07 | `.mcp.json` Postgres path repaired (pointed at the dead laptop path) |
| **2026-08-07** | **Archiving widened** (`archive_all_tour_markets=True`): price + score paths now recorded for EVERY scanned tour h2h market, not only held positions. **This is a hard boundary in the data** — matches archived before this date are held-position-only and carry the entry-policy selection bias; matches after do not. Do not pool them silently in any live-state analysis. Retention: `scripts/prune_price_history.py`. **Live in production 2026-08-08 03:58 UTC** after an API restart; verified in the first 5 minutes: archived markets went 1 → 18, all 18 previously **unheld**, series split KXATPMATCH 10 / KXWTAMATCH 8, zero ITF or outrights, trade count unchanged at 746 |
| 2026-08-07 | `trades.market_id` backfilled 746/746 by pure ticker join (`scripts/backfill_trade_market_id.py`); fix-forward in `TradeService._market_id_for`. Two earlier claims retracted — see [attribution-remediation.md](attribution-remediation.md): `entry_context.region` is on 740/746 (not 2/249), and surface/level are **not** recoverable by join (`kalshi_markets.match_id` is NULL on all 11,533 rows) |
| 2026-08-07 | EXP-14 criterion amended before any result: primary control is the walk-forward **recalibrated** market (M-recal), not the raw ask. Found by harness validation — a pure-noise live input beat the raw market on 1/3 folds via free market recalibration |
